/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import ScrollItems from './tutorial/ScrollItems';

AppRegistry.registerComponent(appName, () => ScrollItems);
